{# <pre>
    {{ dump(app.user) }}
</pre> #}

<nav class="navbar navbar-expand-lg bg-body-tertiary">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path('homepage')}}">Formation</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{path('homepage')}}">Home</a>
				</li>

				{% if app.user %}

					<li class="nav-item">
						<a class="btn btn-primary" aria-current="page" href="{{path('app_logout')}}">Déconnexion</a>
					</li>

				{% else %}

					<li class="nav-item">
						<a class="btn btn-primary" aria-current="page" href="{{path('app_login')}}">Connexion</a>
					</li>

				{% endif %}

				<li>
					<a class="btn btn-primary" aria-current="page" href="{{path('app_inscription')}}">Inscription</a>
				</li>
			</ul>
		</div>
	</div>
</nav>
